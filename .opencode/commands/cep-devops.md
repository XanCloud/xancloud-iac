---
description: CI/CD, GitHub Actions + OIDC, releases, supply chain. INACTIVE in Phase 1 — confirms scope before acting.
agent: devops-engineer
subtask: true
---

Handle the following CI/CD request. First state explicitly that this is
Phase 2+ scope for xancloud-iac (trigger: first paying client) and ask for
confirmation if not already given. Then implement minimally: OIDC over
stored secrets, pinned versions, caching, rollback. No pushes or releases.

$ARGUMENTS
