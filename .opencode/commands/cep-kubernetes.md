---
description: Kubernetes, Helm, EKS work. INACTIVE in Phase 1 — out of project scope; flags conflict before acting.
agent: kubernetes-engineer
subtask: true
---

Handle the following Kubernetes request. First state explicitly that
Kubernetes is out of xancloud-iac Phase 1 scope and ask for confirmation
if not already given. If confirmed, work read-only by default; cluster
mutations are denied.

$ARGUMENTS
