---
description: Convenciones de CI/CD para GitHub Actions
globs: [".github/workflows/**"]
alwaysApply: false
---

# CI/CD — xancloud-iac

## General
- GitHub Actions. El binario es `tofu`, no `terraform`.
- Auth con OIDC (GitHub → AWS IAM Role). NUNCA secrets estáticos de AWS.
- Checkov como quality gate bloqueante.

## Workflows

| Workflow | Trigger | Acción |
|---|---|---|
| module-test.yml | PR a main (modules/**) | tofu fmt, validate, Checkov, tofu test |
| blueprint-validate.yml | PR a main (blueprints/**) | tofu init, plan (mock vars), policy check |
| deploy.yml | Push a main + env label | plan → approval manual → apply |
| drift-detect.yml | Cron (diario) | plan en cada entorno, notifica si hay drift |
| docs-gen.yml | PR a main | terraform-docs auto-commit |

## Reglas
- Apply en prod requiere aprobación manual (GitHub Environments).
- Plan output guardado como artifact para auditoría.
- State access restringido al IAM role del pipeline + admins.
